= semantic_form_for [:admins, @commentable, @commentable.comments.build] do |f|
  = f.hidden_field :user_id, value: current_user.id
  = f.text_area :body, placeholder: "Add a comment"
  = f.submit "Add Comment"
